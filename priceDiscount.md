# priceDiscount — 成本節省試算邏輯

> 用途：計算「若採用不同價格（新調價／換新商品）或運費回沖，成本會差多少」的**參考試算**。
> **全程唯讀**：只讀取資料計算，**不修改**任何訂單成本快照（`shopee_order_items.cost_cny/cost_twd`）、不改商品價格。

---

## 0. 名詞與資料來源

| 名稱 | 表 / 欄位 | 說明 |
|---|---|---|
| 結算價（採購成本） | `bc_products.prices[].settlementPrice`（依 `copies`） | 我們向 BC 採購的單價，依「份數 copies」分階 |
| 價格歷史 | `bc_price_history`（`sync_id, prices, prev_prices, cost_price, prev_cost_price`） | 每次同步的**全量快照**；`prev_*` 為上一次的值 |
| 調價紀錄 | `bc_sync_diffs`（`changed_count, changed[]`） | 每次同步的上下架＋調價統計；`changed` = `[{sku_id, name, old_cost, new_cost}]` |
| 訂單品項 | `shopee_order_items`（`bc_sku_id, matched_copies, quantity, delivery_type, cost_cny, cost_twd`） | 一列一品項；成本是**對應 BC 當下**的快照 |
| 訂單 | `shopee_orders`（`order_date, order_status`） | 篩日期＋排除「不成立」 |
| 匯率 | `exchange_rates`（`currency='CNY'` 的 `rate`，預設 0.2128） | `TWD = CNY / rate`（例：¥35.2 / 0.2128 ≈ NT$166） |

**「份數 copies」= 使用天數**。`settlementPrice` 是「該份數」的單張採購價。

---

## 1. 成本模型（每個品項）

```
單張成本(CNY) = settlementPrice(該品項 matched_copies)
              + (delivery_type 為實體 SIM 時 +¥3 運費；eSIM 不加)
品項成本(CNY) = 單張成本 × quantity
成本(TWD)     = 品項成本(CNY) / rate            // rate 取 exchange_rates CNY，預設 0.2128
```

- `matched_copies` 為空時視為 `'1'`。
- **SIM 判定**：`(delivery_type || 'sim') === 'sim'` → 加 ¥3；`'esim'` → 不加。
- 這與正式對應時後端的算法一致（見 `orders/[id]/route.ts` 對應 BC 帶入成本、`backfill-cost`）。

> ⚠️ 兩情境相減（舊 vs 新）時，**+¥3 運費會互相抵銷**，對「差異」無影響；只影響各自的總額。

---

## 2. 情境 A — 某次「調價」能省多少

**問題**：某次同步把部分 SKU 結算價調整了，若這段期間的訂單都用「新價」，成本差多少。

**步驟**
1. 取該次同步 `sync_id`（例：`2026-09-01 06:59:50 UTC`＝台灣 14:59:50，`changed_count = 320`）。
2. 從 `bc_price_history` 讀該 `sync_id` 的每個 SKU：
   - 舊價 = `prev_prices`（各 copies 的 settlementPrice）
   - 新價 = `prices`
3. 找期間內（`order_date` 區間、排除不成立）用到這些 SKU 的品項。
4. 每品項：
   ```
   舊成本 = (舊價[copies] + ship) × qty
   新成本 = (新價[copies] + ship) × qty
   省下   = 舊成本 − 新成本            // 正=省，負=變貴
   ```
5. 加總 → 淨省下（CNY / TWD），另分「變便宜 / 變貴」。

**「調價」定義**：只要**任一份數**（任何 copies）的結算/零售價有變就算（`priceSig` 比對），但**成本差只看該訂單實際用的 copies**，其他 copies 的變動自然貢獻 0。

**參考結果（2026/01/01–09/30，含 SIM +¥3，排除不成立）**
| | CNY | TWD |
|---|---|---|
| 全部用舊價 | ¥145,416 | NT$683,346 |
| 全部用新價 | ¥144,187 | NT$677,571 |
| **省下** | **¥1,229** | **≈ NT$5,775** |
（實際有變價 489 張：471 張便宜省 ¥1,241、18 張變貴多 ¥12.3）

---

## 3. 情境 B — 換新商品的價格差異（U Mobile）

**問題**：把舊商品換成同規格的新商品，若用新商品價格，成本差多少。

### 3.1 對應表怎麼建
以 **product_id** 精準鎖定兩個產品，再依「規格鍵」自動配對：

```
規格鍵 = 類型(總量/每日) | 容量(如 1GB / 500MB) | 降速(5Mbps / 384kbps) | 載體(SIM/eSIM)
  類型：名稱含「总量」→ 總量；含「每日」→ 每日
  容量：抓 (\d+)GB 或 (\d+)MB
  降速：含「降速5Mbps」→ 5Mbps；否則視為 384kbps（新商品名稱未標降速＝標準 384kbps 檔）
  載體：含「esim / 载体」→ eSIM；否則 SIM
```

**範例（本次）**：舊 `新加坡马来西亚`（product_id **1061**）→ 新 `马来西亚 / U Mobile本地`（product_id **1016**），配到 **22 組**（SIM 11 + eSIM 11）。
- 舊 `每日1GB-降速384kbps` ↔ 新 `每日1GB`（新商品無降速字樣＝384kbps）
- 舊 `每日1GB-降速5Mbps` ↔ 新 `每日1GB-降速5Mbps`
- 舊 `总量XGB-降速384kbps` ↔ 新 `总量XGB`
- 沒配到：舊有「全程10Mbps」新沒有；新多出「每日500MB、总量15GB」。product_id 1016 底下的「马来西亚-自选」系列非 U Mobile本地，已排除。

### 3.2 差異怎麼算
對期間內用到**舊 SKU** 的品項：
```
舊成本 = (舊SKU價[copies] + ship) × qty
新成本 = (新SKU價[copies] + ship) × qty   // 用對應表換到新 SKU 的價格
省下   = 舊成本 − 新成本
```

**參考結果 B（2026/01/01–09/30，成立，含 SIM +¥3）**
| | CNY | TWD |
|---|---|---|
| 舊（新加坡马来西亚） | ¥18,347 | NT$86,219 |
| 新（U Mobile） | ¥14,777 | NT$69,441 |
| **用新商品省下** | **¥3,570** | **≈ NT$16,779（−19.5%）** |
（涉及 623 張卡；22 組規格**全面較便宜**，無變貴）

---

## 4. 情境 C — 欧洲33国 → 欧洲33国B

同樣是「換新商品」（沿用 §3 的規格鍵配對法），但有兩點特別：

- **同一 product_id（1331）**，兩者靠**名稱前綴**分：`欧洲33国-自选`＝舊、`欧洲33国B`＝新。
- B 版把**低速 128kbps 升到 384kbps 且更便宜**；`5mbps / 10mbps` 檔維持不變。
- 因此規格鍵的「降速」要**歸類**：`128kbps` 與 `384kbps` → 同一類 `std`（舊 128 ↔ 新 384 對應）；`5mbps→5m`、`10mbps→10m` 各自對應。配到 28 組（舊「每日800MB」「無限總量(純)」B 版無對應）。

**參考結果 C（2026/01/01–09/30，成立，含 SIM +¥3）**
| | CNY | TWD |
|---|---|---|
| 舊（欧洲33国） | ¥3,177 | NT$14,929 |
| 新（欧洲33国B） | ¥1,891 | NT$8,888 |
| **用 B 版省下** | **¥1,286** | **≈ NT$6,041（−40.5%）** |
（涉及 62 張卡；各規格全面較便宜，降幅最猛）

---

## 5. 情境 D — 每張卡退 ¥3 運費

**問題**：實體 SIM 的 ¥3 運費若回沖，等於每張省 ¥3。

**步驟**
1. 期間內成立訂單的品項，加總 `quantity`：
   - 總張數、SIM（實體）張數、eSIM 張數。
2. 退費 = **¥3 × SIM 張數**（運費只加在實體卡）。

**參考結果（2026/01/01–09/30，排除不成立）**
- 訂購總張數 **3,906**（SIM 3,749 / eSIM 157）；已回填 ICCID 3,093。
- 退 SIM 實體卡：3,749 × ¥3 = **¥11,247 ≈ NT$52,852**。
- 若退全部卡：3,906 × ¥3 = ¥11,718 ≈ NT$55,066。

---

## 6. 共通規則 / 注意

- **範圍**：`order_date` 落在指定區間；`order_status` 排除「不成立」（NULL 視為成立）。
- **對象**：只算有 `bc_sku_id` 對應、有 `matched_copies` 的品項。
- **張數**：以品項 `quantity` 加總（＝訂購張數）；成本亦以單張 × quantity。
- **抓不到價**：某 SKU 在該 `copies` 沒有結算價 → 該筆略過並回報張數，不硬湊。
- **唯讀**：以上皆為試算，不寫回 `shopee_order_items`、不改 `bc_products`。
- **重新對應才會變**：訂單成本是快照；BC 調價/同步**不會**回頭改訂單成本，只有「重新對應」或手動改才會用當下價格覆蓋。

---

## 7. 各情境省下（本次參考）

| 項目 | 省下(CNY) | 省下(TWD) | 比例 |
|---|---|---|---|
| A. 9/1 調價（新價 vs 舊價，489 張有變） | ¥1,229 | 5,775 | −0.85%（對該批舊價）|
| B. 換 U Mobile 新商品（623 張） | ¥3,570 | 16,779 | −19.5%（對舊 SKU 成本）|
| C. 換 欧洲33国B 新商品（62 張） | ¥1,286 | 6,041 | −40.5%（對舊 SKU 成本）|
| D. 每張退 ¥3 運費（SIM 3,749 張） | ¥11,247 | 52,852 | — |

**比例算法**：`差異 ÷ 舊成本`（省下的百分比）；「新為舊的 X%」＝ `新 ÷ 舊`。

### 7.1 A + B + C 去重後的整體（採購價優化）

- A（調價）、B（U Mobile）、C（欧洲33国B）用的是**不同 SKU、不重疊**（去重後 ≈ 直接相加），可加。
- 整體（2026/01/01–09/30、成立、含 SIM +¥3、依規格結算價重算）：

| | CNY | TWD |
|---|---|---|
| 舊成本合計 | ¥147,102 | NT$691,386 |
| 新成本合計 | ¥140,996 | NT$662,756 |
| **省下** | **¥6,093** | **NT$28,630（−4.14%）** |

> 這裡的總額（691,386）＝依 BC 規格結算價重算，與頁面「商品成本 633,483」口徑不同（頁面是**對應當下的快照**）。

### 7.2 四項全套 vs 原本總成本

- D（退運費）與 A/B/C（採購價）為**不同面向、互不重複**，四者可直接相加。
- 原本總商品成本以**儀表板現況**為基準（已結算 ＋ 未結算，已含現運費）：

| | TWD |
|---|---|
| 原本總商品成本（561,667 ＋ 71,816） | **NT$633,483** |
| 四項省下合計（A 5,775 ＋ B 16,779 ＋ C 6,041 ＋ D 52,852） | − NT$81,447 |
| **優化後新成本** | **≈ NT$552,036** |
| **差異 / 比例** | **省 NT$81,447（−12.86%）** |

> 大頭是 **D 退運費 NT$52,852（約佔省下 65%）**；A/B/C 為採購價優化（B、C 是換更便宜的新商品，降幅最大）。
> 原本總成本已包含現行 ¥3 運費，故 D 是實質扣除。
