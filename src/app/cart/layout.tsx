import type { Metadata } from 'next'
export const metadata: Metadata = { title: '購物車', robots: { index: false } }
export default function L({ children }: { children: React.ReactNode }) { return children }
